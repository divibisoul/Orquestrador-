# Architecture contract

This top-level document is the short constitutional contract. The detailed specification is in `docs/ARCHITECTURE.md`.

The system separates state, executive cognition, orchestration, intelligence, mesh and cross-cutting security/observability. Core modules do not depend directly on vendor-specific infrastructure.
