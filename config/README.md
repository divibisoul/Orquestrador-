# Configuration

`nexus.yaml` contains safe development defaults and benchmark targets. Production credentials and private endpoints must be injected through the deployment secret manager, never committed.
