name: Security

on:
  push:
    branches: [main, recovery/consolidation]
  pull_request:

permissions:
  contents: read

jobs:
  gosec:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: securego/gosec@v2.22.8
        with:
          args: ./...
