name: Benchmark
on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * 0'
jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'
      - run: go test ./... -bench=. -run '^$'
