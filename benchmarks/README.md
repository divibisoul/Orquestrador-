# Benchmarks

Measure decision P95, inference throughput, resource utilization, power where hardware telemetry is available, fault recovery, and node scale-out. Results must record hardware, model, precision, payload size, network topology and software revision.

Target values in the architecture are hypotheses/SLO goals until reproduced by this benchmark suite.
