# Implementation map

The 60 constitutional operations are implemented as methods in three core packages for the foundation release. Directory-level READMEs preserve the requested physical module boundaries while avoiding duplicate implementations.

The next phase should split the consolidated packages into the individual submodules only when doing so improves testability or dependency isolation.
