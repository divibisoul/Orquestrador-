# Protocols

`api/proto/` defines the stable wire contracts. The design uses capability negotiation so a node may support only the transports and model operations it can actually execute.

Compatibility metadata is advisory until policy validation and health checks succeed.
