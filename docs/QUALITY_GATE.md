# Quality gate

Before merging the foundation: `go vet ./...`, `go test ./...`, benchmark baseline, security scan, contract review and manual verification that no performance target is presented as proven without measurements.
